{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimal capabilities for the local Studio window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "window-state:default"
  ]
}
